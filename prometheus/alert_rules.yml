groups:
  - name: application
    rules:
      - alert: TauxErreurEleve
        expr: >
          sum(rate(http_requests_total{status=~"5.."}[1m]))
          / sum(rate(http_requests_total[1m])) > 0.05
        for: 30s
        labels:
          severity: warning

      - alert: ApplicationInjoignable
        expr: up{job="flask-app"} == 0
        for: 15s
        labels:
          severity: critical
